Is a utility tool shipped with prometheus that can be used to:
- Check and validate configuration
  validate prometheus.yml
  Validate rules files

And more

Example:
promtool check config /path/to/prometheus.yml